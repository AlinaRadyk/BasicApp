import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  button: {
    height: '100%',
    width: 50,
    justifyContent: 'center',
    alignItems: 'center',
  },
  iconSize: {
    fontSize: 30,
  },
});
